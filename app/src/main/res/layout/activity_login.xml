<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/activity_login"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app.background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="kz.akmarzhan.nationaltest.views.authentication.LoginActivity">

    <EditText
        android:id="@+id/et_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="80dp"
        android:hint="@string/login.login.hint"
        android:textColor="@color/text.white.primary"
        android:textColorHint="@color/text.white.hint"
        />

    <EditText
        android:id="@+id/et_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_login"
        android:layout_marginTop="16dp"
        android:hint="@string/login.password.hint"
        android:inputType="textPassword"
        android:textColor="@color/text.white.primary"
        android:textColorHint="@color/text.white.hint"/>

    <android.support.v7.widget.AppCompatButton
        android:id="@+id/bt_login"
        style="@style/AppTheme.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_password"
        android:layout_marginTop="24dp"
        android:text="@string/login.button.login.text"
        android:textColor="@color/text.white.primary"
        app:backgroundTint="@color/colorAccent"
        />

    <android.support.v7.widget.AppCompatButton
        android:id="@+id/bt_register"
        style="@style/AppTheme.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/login.button.register.text"
        android:textColor="@color/text.white.primary"
        app:backgroundTint="@color/colorAccent"
        />

</RelativeLayout>
